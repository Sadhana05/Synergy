# Complete .env file should look like this:
# Database Configuration
MONGODB_URI=mongodb+srv://synergy:synergy@synergy.r8btvlg.mongodb.net/?appName=synergy
MONGODB_DB_NAME=synergy

# Auth Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_ORIGIN=http://localhost:5173
PORT=3001
PUBLIC_BASE_URL=http://localhost:3001

# OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Deployment build timeout (milliseconds)
DEPLOY_BUILD_TIMEOUT_MS=300000

# Terminal Container Image (must include node, python, gcc/g++, javac/java)
DOCKER_TERMINAL_IMAGE=mcr.microsoft.com/devcontainers/universal:2

# Groq AI Configuration
GROQ_API_KEY=your_groq_api_key
GROQ_MODEL=llama-3.3-70b-versatile
GROQ_BASE_URL=https://api.groq.com/openai/v1/chat/completions

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# MongoDB connection helpers
# Set to true if your network cannot connect to Atlas over IPv6.
MONGODB_FORCE_IPV4=true

# Gmail SMTP Configuration
GMAIL_EMAIL=your_email@example.com
GMAIL_PASSWORD=your_app_password
